ccxt[pro]
fastapi
uvicorn[standard]
rich
pandas
pyarrow
requests
# Windows bringt keine Zeitzonendatenbank mit; ohne tzdata faellt der
# Event-Layer auf festes UTC-5 zurueck (siehe core/event_layer.py).
tzdata; platform_system == "Windows"
